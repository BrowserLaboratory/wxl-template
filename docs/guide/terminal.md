# Terminal Usage Guide

<!-- TODO: full content migration in change content-i18n-migration -->

This guide documents the Terminal panel, the built-in `wxlsh` shell commands (`base64`, `hex`, `curl`, `encode`, `decode`), command history, and keyboard shortcuts.

The English version of this page is a placeholder. The full Traditional Chinese version is available at [`/zh-TW/guide/terminal`](/zh-TW/guide/terminal).
