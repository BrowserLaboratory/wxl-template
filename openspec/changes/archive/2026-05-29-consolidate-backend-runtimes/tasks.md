## 1. 收斂 backend runtime spec 為 challenge-runtimes

- [x] 1.1 [P] 核對 `challenge-runtimes` spec delta 完整保留三來源 spec 全部行為，逐條涵蓋下列 12 條 Requirement、30 個 Scenario 無遺漏：Challenge runtime initializes once per challenge session、Challenge runtime mounts the virtual filesystem before executing app code、Challenge runtime modules reside in .vitepress/theme/composables、Python ASGI runtime translates HTTP requests to WSGI/ASGI and invokes the app、Python ASGI runtime collects response events and returns an HTTP response、Python ASGI runtime initialize installs micropip packages before app execution、Python ASGI runtime handles cookie and header transport on request dispatch、Python ASGI runtime supports FastAPI apps with BASE_PACKAGES and the packages frontmatter、Python ASGI runtime E2E test mocks are complete、PHP runtime executes challenge PHP code via php-wasm with request context、PHP runtime populates request superglobals from method and body、PHP runtime initialize accepts app code and FS entries。驗證：`rg -c "^### Requirement:" openspec/changes/consolidate-backend-runtimes/specs/challenge-runtimes/spec.md` 回傳 12，且逐一對照 python-asgi-runtime／php-runtime／fastapi-challenge 原 Scenario 清單確認無缺。
- [x] 1.2 [P] 確認本 change 為純 spec 重構、無任何 runtime 程式碼變動：runtime 實作模組與其單元／E2E 測試皆不更動。驗證：`git status` 無任何 `.ts`／`.vue` 變更；`pnpm test` 全綠（若本機可跑）。
- [x] 1.3 移除三個被取代的 capability spec 目錄（`python-asgi-runtime`、`php-runtime`、`fastapi-challenge`），使其不再以獨立 capability 存在、行為改由 challenge-runtimes 承載。驗證：三目錄不存在於 `openspec/specs/`；`rg -l "(python-asgi-runtime|php-runtime|fastapi-challenge)" openspec/specs/ -g '!**/challenge-runtimes/**'` 回傳 0 命中（code 檔名巧合不計）。
