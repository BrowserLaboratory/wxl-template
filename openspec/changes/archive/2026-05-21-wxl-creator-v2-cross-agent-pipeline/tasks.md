<!--
每條 task 都必須同時帶：(a) 完成後可觀察的行為 / 契約；(b) 用什麼方式驗證完成（測試名稱、CLI 呼叫、analyzer 檢查、手動斷言、或產出檔內容審閱）。
檔案路徑只是定位用的上下文，不是 task 本身 — 不接受「修改 X 檔」這種純路徑描述。
locale: tw — 任務描述用繁體中文；但技術 token（path、commands、Requirement 名稱、symbol、code identifier）保留原文以利 analyzer 子字串比對。
本變更採 parallel_tasks: true — 可並行的任務以 [P] 標示。
-->

## 1. 基礎依賴與目錄骨架（establishing the runtime/build foundation）

- [x] 1.1 在 `package.json` 內以 devDependency 形式登錄 `@playwright/test`（鎖 latest stable），並新增三個 scripts：`challenge:retype` 指向 `tsx scripts/challenge-retype.ts`、`challenge:verify` 指向 `tsx scripts/challenge-verify.ts`、`challenge:verify:blind` 指向 `tsx scripts/challenge-verify-blind.ts`；落實 Decision 8「Harness 全程 TypeScript、不引入 Python」— 三支新 CLI 一律 `.ts` + tsx runner、嚴禁新增 `requirements.txt`、`pyproject.toml`、或任何 Python 依賴宣告；行為交付 = 「Playwright is a devDependency」Requirement 落地、新 CLI 可由 pnpm 入口呼叫；驗證 = 跑 `pnpm install` 後 `node_modules/@playwright/test/package.json` 存在、`pnpm challenge:retype --help` 不再回 unknown script、本 task 完成後 `git status` 不見任何 Python build artefact。
- [x] 1.2 [P] 在 `.gitignore` 末段加入 `tmp/wxl-verify/` 規則；行為交付 = 「Ephemeral artefact cleanup」Requirement 的「player package 不入 git」前置條件；驗證 = 手動建 `tmp/wxl-verify/test/dummy.txt`、跑 `git status` 不見此檔。
- [x] 1.3 [P] 在 `README.md` 的 Prerequisites 區塊新增「首次跑 `pnpm challenge:verify` 前需執行 `pnpm exec playwright install chromium`」、Available scripts 表新增 `challenge:retype`、`challenge:verify`、`challenge:verify:blind` 三列；行為交付 = README 涵蓋「Playwright is a devDependency」Requirement 文件層；驗證 = grep README 應含 `pnpm exec playwright install chromium` 與三個新 script 名稱。
- [x] 1.4 [P] 在 `CONTRIBUTE.md` 的 Adding a new challenge 段補上 Mutate / Verify gate 兩節，並把舊章節「pnpm challenge:validate」改寫成「pnpm challenge:verify」、附說明「verify 是 release-blocking gate」；行為交付 = 出題者讀 CONTRIBUTE.md 即可知三階段流程；驗證 = grep CONTRIBUTE.md 應含 `pnpm challenge:retype`、`pnpm challenge:verify`、`Verify gate`。

## 2. Skill 真實來源遷移與 thin pointer（Decision 1, 8 + Skill Cross-Agent Layout 契約）

- [x] 2.1 建立 `.agent/skills/wxl-creator/` 目錄並把現有 `.claude/skills/wxl-creator/SKILL.md` 整份內容**複製**到 `.agent/skills/wxl-creator/SKILL.md`，作為 host-agent-neutral 化的起點；行為交付 = 「Skill is installed via a single source with thin pointer files」Requirement 的真實來源檔案存在、且初始內容跟原 Claude 版等價；驗證 = `diff <(sed 's/^---name: wxl-creator/---/' .agent/skills/wxl-creator/SKILL.md) .claude/skills/wxl-creator/SKILL.md` 在「未做 host-agent-neutral 改寫」前應只差 frontmatter；改寫完成後改用下一條 task 的 grep check 驗證。
- [x] 2.2 重寫 `.agent/skills/wxl-creator/SKILL.md` 內容：移除所有 `AskUserQuestion` 呼叫、改為 plain-text 提問格式（依 `Skill collects challenge parameters interactively` MODIFIED Requirement 所示 emoji + 三行選項模板）；行為交付 = 「Skill prose is host-agent-neutral」Requirement 的核心轉變、實現 Decision 2「移除 AskUserQuestion、改成 plain-text 提問」；驗證 = `git grep -nE 'AskUserQuestion|EnterPlanMode|ExitPlanMode|TaskCreate|TaskUpdate|subagent_type' .agent/skills/wxl-creator/` 回 exit 1（無匹配）。
- [x] 2.3 [P] 在 `.agent/skills/wxl-creator/` 目錄新增 `SKILL.zhTW.md`，內容與 `SKILL.md` 對齊但用繁中表達（technical token 仍保留原文）；行為交付 = 繁中讀者也有對等 skill 文件、Decision 1「.agent 為單一來源」結構完整；驗證 = 章節數量與標題順序對齊 SKILL.md（`grep -c '^##' SKILL.md` 應等於 SKILL.zhTW.md）。
- [x] 2.4 [P] 新增 `.agent/skills/wxl-creator/AGENTS.md`，說明三家 host agent runtime（Claude Code / Codex CLI / Gemini CLI）如何 discover 此技巧、何時被呼叫、與其他 skill 的關係，並對應 design 內「跨家 skill 安裝契約 / Skill Cross-Agent Layout」一節；行為交付 = 跨家 dispatcher 文件存在、辨識 skill 入口流程；驗證 = 內容應出現「Claude Code」、「Codex CLI」、「Gemini CLI」三家名稱與各自 skill discovery 路徑。
- [x] 2.5 [P] 新增 `.agent/skills/wxl-creator/reference/agent-tools.md`，列出三家共有的 tool 名稱對照（Bash / Read / Write / Edit / Glob / Grep / WebFetch）、與各 runtime 對 chrome-devtools-mcp 的支援成熟度註記；行為交付 = host-agent-neutral 限制在文件層有清楚邊界（落實 Decision 2 與「Host-agent-neutral prose 契約」）；驗證 = 內容應出現 7 個共有 tool 名稱與 chrome-devtools-mcp 的三家狀態欄。
- [x] 2.6 把 `.claude/skills/wxl-creator/SKILL.md` 改寫為 thin pointer：保留 frontmatter `name: wxl-creator` + `description`，body 改為「Read .agent/skills/wxl-creator/SKILL.md for the canonical skill content.」一句；行為交付 = 「Skill is installed via a single source with thin pointer files」對 Claude Code 路徑落地、Decision 1 完整；驗證 = file 行數 ≤ 8、body 含 `Read .agent/skills/wxl-creator/SKILL.md` 字串。
- [x] 2.7 [P] 新增 `.codex/skills/wxl-creator/SKILL.md` 為 thin pointer（內容形式同 2.6，按 Codex CLI 慣例調整 frontmatter）；行為交付 = Codex CLI 路徑下能 discover 並透過 pointer 載入真實 prose；驗證 = file 存在、body 含 `Read .agent/skills/wxl-creator/SKILL.md` 字串。
- [x] 2.8 [P] 新增 `.gemini/skills/wxl-creator/SKILL.md` 為 thin pointer（內容形式同 2.6，按 Gemini CLI 慣例調整 frontmatter）；行為交付 = Gemini CLI 路徑下能 discover 並透過 pointer 載入真實 prose；驗證 = file 存在、body 含 `Read .agent/skills/wxl-creator/SKILL.md` 字串。

## 3. Mutate 階段 CLI — `scripts/challenge-retype.ts`（Decision 3 + Mutate CLI 契約）

- [x] 3.1 在 `scripts/challenge-retype.ts` 新檔內實作 CLI argument parser（使用 `node:util` parseArgs），接受 `--backend / --difficulty / --tags / --category` 並支援多 flag 組合；行為交付 = 「Challenge retype script」Requirement 的 CLI 入口契約落地（Decision 3「Mutate 階段拆獨立 CLI」）；驗證 = `pnpm challenge:retype door-is-open --difficulty hard --tags 'a,b'` 不報 parse error 且能列印解析結果（暫以 console.log 替代後續實作）。
- [x] 3.2 在 `scripts/challenge-retype.ts` 實作純 frontmatter mutation 路徑（不帶 `--backend` 時）：讀 `docs/challenge/<slug>/index.md`、解 YAML frontmatter、套用 difficulty / tags / category 變更後寫回、保留原檔其他內容不動；行為交付 = 「Challenge retype script」對 difficulty/tags/category 唯有 metadata 變更的 sub-behavior；驗證 = Vitest unit test `tests/unit/scripts/challenge-retype-metadata.test.ts` 以 fixture 驗 round-trip frontmatter 內容。
- [x] 3.3 在 `scripts/challenge-retype.ts` 實作同語言系 backend 變更（flask ↔ fastapi）：rewrite `src/app.py` 的 import / framework bootstrap header、保留 route handler / SQL / template 等 vuln 主體、更新 frontmatter `backend` 與 `packages` 後重跑 `pnpm challenge:keygen <slug>`；行為交付 = 「Challenge retype script」同語言 backend 變更 sub-behavior；驗證 = Vitest `tests/unit/scripts/challenge-retype-same-family.test.ts` 對 door-is-open fixture 跑 fastapi→flask 後比對 vuln 主體 hash 未變、import line 已變。
- [x] 3.4 在 `scripts/challenge-retype.ts` 實作跨語言系 backend 變更（python 系 ↔ php）：嘗試 best-effort 轉寫；不可保留 vuln 邏輯時以 exit code 2 拒絕、列「manual retype required: <reason>」、確保檔案系統未動；行為交付 = 「Challenge retype script」跨語言 fail-fast 契約；驗證 = Vitest `tests/unit/scripts/challenge-retype-cross-family.test.ts` 對 door-is-open 跑 fastapi→php、斷言 exit code 2 + 「manual retype required」訊息 + git status clean。
- [x] 3.5 在 `scripts/challenge-retype.ts` 加 spec.ts EXPLOIT_PATH 同步邏輯：backend 變更後若 `tests/challenges/<slug>.spec.ts` 內 EXPLOIT_PATH 可由新 framework convention 推導，自動更新；不可推導時保留原值、印 warning；行為交付 = Mutate 後 Playwright spec 與新 backend 對齊（「Challenge retype script」契約最後一條）；驗證 = Vitest `tests/unit/scripts/challenge-retype-spec-sync.test.ts` 對 door-is-open 跑 fastapi→flask、確認 spec.ts 內 EXPLOIT_PATH 仍有效或得 warning。
- [x] 3.6 [P] 在 `scripts/challenge-retype.ts` 加錯誤路徑：找不到 slug → exit 1；未知 backend 值 → exit 1 列接受值；keygen 失敗 → exit 3 保留現場；行為交付 = 「Challenge retype script」全套 exit code 契約；驗證 = Vitest `tests/unit/scripts/challenge-retype-errors.test.ts` 對三種錯誤情境分別斷言 exit code 與 stderr 訊息。

## 4. Verify 階段 CLI — `scripts/challenge-verify.ts` 與 L1–L3 串接（Decision 4, 7 + Verify CLI 契約）

- [x] 4.1 在 `scripts/challenge-verify.ts` 新檔內實作 CLI argument parser、接受 `--blind`、`--layers <L1,L2,L3,L4>`、`--json` 三類旗標；行為交付 = 「Challenge verify script with layered gates」CLI 入口契約落地（Decision 4「Verify pipeline 採 L1-L4 四層 gate」）；驗證 = `pnpm challenge:verify door-is-open --blind --layers L1,L4 --json` 不報 parse error 並能列印解析後參數（暫以 stub）。
- [x] 4.2 在 `scripts/challenge-verify.ts` 串接 L1：呼叫既有 `validateChallenge()` export，把回傳結果轉成 layered status 行；行為交付 = 「Challenge verify script with layered gates」L1 sub-behavior；驗證 = Vitest `tests/unit/scripts/challenge-verify-L1.test.ts` 對 door-is-open fixture 斷言「✓ L1 passed」單行 stdout。
- [x] 4.3 在 `scripts/challenge-verify.ts` 串接 L2：呼叫既有 analyze 邏輯 + `pnpm challenge:keygen <slug>` + `wasm-tools validate docs/challenge/<slug>/runtime.wasm`，任一步 fail 即 L2 fail；行為交付 = 「Challenge verify script with layered gates」L2 sub-behavior、含 wasm-tools 整合；驗證 = Vitest `tests/unit/scripts/challenge-verify-L2.test.ts` mock 三步驟、斷言三者皆 OK 時 stdout `✓ L2 passed`，任一 fail 時 stdout `✗ L2 failed: <reason>`。
- [x] 4.4 在 `scripts/challenge-verify.ts` 串接 L3：透過 `child_process.spawn` 呼 `pnpm exec playwright test tests/challenges/<slug>.spec.ts`、解析 exit code 與 stderr；行為交付 = 「Challenge verify script with layered gates」L3 sub-behavior、Decision 7「L3 e2e 採 Playwright」落地；驗證 = Vitest `tests/unit/scripts/challenge-verify-L3.test.ts` 以 fake playwright（spawn mock）斷言 spec 路徑被正確組裝、exit code 傳遞。
- [x] 4.5 在 `scripts/challenge-verify.ts` 串接 L4：當帶 `--blind` 時呼叫 `scripts/challenge-verify-blind.ts` 並把 exit code 傳出；行為交付 = 「Challenge verify script with layered gates」L4 dispatch sub-behavior；驗證 = Vitest `tests/unit/scripts/challenge-verify-L4-dispatch.test.ts` mock blind 子程序、斷言只有帶 --blind 才呼叫、否則跳過。
- [x] 4.6 在 `scripts/challenge-verify.ts` 加入 layer orchestration + fail-fast：依序 L1→L2→L3（→L4），任一 fail 就停、最後一行印 `verified: <slug> (<layers>)` 或 `failed: <slug> at L<n>` 或 `inconclusive: <slug> at L4`；行為交付 = 「Challenge verify script with layered gates」總線 + Decision 4 fail-fast 行為；驗證 = Vitest `tests/unit/scripts/challenge-verify-orchestration.test.ts` 注入 L1 pass + L2 fail 情境、斷言 L3/L4 未被呼叫且 stdout 末行為 `failed: <slug> at L2`。
- [x] 4.7 [P] 在 `scripts/challenge-verify.ts` 加 `--json` 輸出模式：依 Verify CLI 契約 JSON shape 把全部 layer 結果 serialize；行為交付 = 「Challenge verify script with layered gates」CI 整合 sub-behavior；驗證 = Vitest `tests/unit/scripts/challenge-verify-json.test.ts` 對 layered 結果做 round-trip 解析、斷言 keys = `slug,layers_run,results,summary,failed_at`。
- [x] 4.8 [P] 在 `scripts/challenge-verify.ts` 加 `--layers` 過濾：把使用者指定的子集依序跑、其他 skip；行為交付 = 「Challenge verify script with layered gates」debug-friendly sub-behavior；驗證 = Vitest `tests/unit/scripts/challenge-verify-layers-filter.test.ts` 跑 `--layers L1,L3` 斷言 L2/L4 未被呼叫。

## 5. L4 Blind solve 子系統 — `scripts/challenge-verify-blind.ts` 與 `scripts/wxl-solver/*.ts`（Decision 5, 6 + L4 Blind-solve 契約）

- [x] 5.1 在 `scripts/wxl-solver/build-player-package.ts` 實作 player-package builder：讀 `docs/challenge/<slug>/index.md`、strip frontmatter 與 maintainer-only 區塊、輸出 description.md（H1 + 緊跟段落）+ META.yaml（base_url / flag_regex / turn_budget / verification_run_id）到 `tmp/wxl-verify/<slug>/player-package/`；行為交付 = 「Player package construction in ephemeral working directory」Requirement 落地（Decision 6 player-package 結構）；驗證 = Vitest `tests/unit/scripts/wxl-solver/build-player-package.test.ts` 以多種 index.md 形式 fixture 跑、斷言 player-package 只含兩個檔且不含 src/ 內容 bytes。
- [x] 5.2 在 `scripts/wxl-solver/spawn-runtime.ts` 實作 runtime CLI dispatcher：依 `WXL_VERIFY_RUNTIME` 環境變數（預設 claude）組對應 CLI 命令（依 `.agent/skills/wxl-creator/reference/runtime-cli.md` 對照表）、用 `child_process.spawn` 啟動、stdout 寫進 `tmp/wxl-verify/<slug>/run.log`；行為交付 = 「Runtime CLI dispatch via environment variable」Requirement 落地（Decision 5「L4 採 spawn fresh agent CLI session」）；驗證 = Vitest `tests/unit/scripts/wxl-solver/spawn-runtime.test.ts` mock spawn、對三家 runtime 各驗一次 command-line 組裝正確；對未知 runtime 值斷言 exit 1。
- [x] 5.3 在 `scripts/wxl-solver/extract-flag.ts` 實作 final_flag 解析：對 stdout 最後 20 行做 regex `^FINAL_FLAG=(.+)$` 比對、取最後一個 match、回傳字串或 null；行為交付 = 「Final flag extraction and byte-comparison」Requirement 第一階段 — 從 agent output 抽 flag；驗證 = Vitest `tests/unit/scripts/wxl-solver/extract-flag.test.ts` 多種 stdout（含正常 / 多 match / 無 match / INCONCLUSIVE）斷言抽出值。
- [x] 5.4 在 `scripts/wxl-solver/extract-flag.ts` 補 byte-compare 與 verdict 邏輯：讀 `docs/challenge/<slug>/src/flag.txt`（trim trailing newline）、與 extract 結果比對、依「Final flag extraction and byte-comparison」Requirement 規則回 verdict（pass / fail / inconclusive）+ reason；行為交付 = 「Final flag extraction and byte-comparison」Requirement 第二階段 — 對 canonical flag 比對；驗證 = Vitest `tests/unit/scripts/wxl-solver/extract-flag-compare.test.ts` 覆蓋四 scenarios（match / regex fail / canonical mismatch / null）。
- [x] 5.5 在 `scripts/challenge-verify-blind.ts` 把 5.1+5.2+5.3+5.4 與 prompt template 串成完整流程：pre-flight 確認 dev server 起在 localhost:5173、build player-package、spawn runtime（帶 prompt）、extract flag、byte-compare、輸出 verdict + 對應 exit code（0 pass / 1 fail / 2 inconclusive）；行為交付 = L4 Blind-solve 契約完整端到端；驗證 = Vitest `tests/unit/scripts/challenge-verify-blind-orchestration.test.ts` mock 各子模組、注入三種 verdict 情境、斷言 exit code 與 stdout 末行內容。
- [x] 5.6 在 `.agent/skills/wxl-creator/reference/runtime-cli.md` 補三家 CLI 對照表（claude --print / codex exec / gemini -p 的完整 flag 組合與 stdout JSON shape 提示），並標 Gemini MCP 成熟度警告；行為交付 = 「Runtime CLI dispatch via environment variable」Requirement 文件對照、Risks 章節「三家 CLI 行為差異」mitigation；驗證 = grep 此檔應出現三家命令名稱、三個 `--max-turns 30` 範例、與 Gemini MCP caveat 字串。
- [x] 5.7 在 `scripts/challenge-verify-blind.ts` 內把 prompt template 寫成常數，內含「Target / Description / Flag format / chrome-devtools-mcp / FINAL_FLAG=」五個必含子字串、與 fabrication 禁止句；行為交付 = 「Prompt template instructs agent to emit a final flag line」Requirement 落地；驗證 = Vitest `tests/unit/scripts/challenge-verify-blind-prompt.test.ts` 對組好的 prompt 字串斷言五個 substring 與 fabrication 禁止句存在。
- [x] 5.8 在 `scripts/challenge-verify-blind.ts` 加結束時 cleanup：用 `fs.rm(tmp/wxl-verify/<slug>/, { recursive: true, force: true })` 刪整個 ephemeral 目錄、刪除失敗只 warn 不改 exit code；行為交付 = 「Ephemeral artefact cleanup」Requirement 落地（Decision 6「verify 結束即刪」）；驗證 = Vitest `tests/unit/scripts/challenge-verify-blind-cleanup.test.ts` 跑 dummy verify、斷言目錄消失、模擬刪除失敗時 exit code 不變。

## 6. Playwright spec 自動生成（templates/exploit-spec.ts.tmpl + skill 寫檔行為）

- [x] 6.1 在 `.agent/skills/wxl-creator/templates/exploit-spec.ts.tmpl` 新檔內寫 mustache-style template（含 `{{SLUG}} / {{BASE_URL}} / {{EXPLOIT_PATH}} / {{EXPLOIT_PAYLOAD}} / {{FLAG_REGEX}}` 五個 placeholder），確保 render 後是合法 TS + 直接可被 `pnpm exec playwright test` 執行；行為交付 = 「Skill generates a Playwright e2e spec for each new challenge」Requirement 模板基礎、Decision 7 落地；驗證 = 對 template 跑 5-key 替換產出 spec、用 `tsc --noEmit` 型別檢查 + `pnpm exec playwright test <produced>.spec.ts --list` 不報語法錯。
- [x] 6.2 在 `.agent/skills/wxl-creator/SKILL.md` 補 Create 階段「寫 spec.ts」步驟：明確要求讀 template、做 5 key 字串替換、寫到 `tests/challenges/<slug>.spec.ts`、template 不存在則 halt 並刪除任何已寫的中間檔；行為交付 = 「Skill generates a Playwright e2e spec for each new challenge」Requirement 操作層落地、對應「Create 階段 spec 自動生成契約」；驗證 = 手動 walkthrough — 對虛擬 slug `xss-basic` 跑 skill Create flow、確認 `tests/challenges/xss-basic.spec.ts` 寫出且 grep 出現「expect(...).toMatch(FLAG_REGEX)」。
- [x] 6.3 [P] 新增 `tests/challenges/door-is-open.spec.ts` 作為 canonical 範例 spec（用 template render 出來、針對 IDOR 對應 endpoint 寫 fetch）；行為交付 = canonical 題目有對應 spec、可作為後續 Verify L3 acceptance 基準；驗證 = `pnpm exec playwright test tests/challenges/door-is-open.spec.ts` 在 dev server 啟動時 pass、未啟動時 fail 明確。

## 7. Create 階段 chrome-devtools-mcp 自我驗證（Decision 9 + Create 階段 chrome-devtools-mcp 自我驗證契約）

- [x] 7.1 在 `.agent/skills/wxl-creator/SKILL.md` 補 Create 階段「best-effort self-test」步驟：load BASE_URL、等 Service Worker ready、發 exploit、確認 response 含 flag；上限 3 次、每次失敗最多改 1 次 vuln code；MCP 不可用 / dev server 不在 / 第三次仍 fail 皆 graceful degrade；行為交付 = 「Skill performs best-effort exploit self-test via chrome-devtools-mcp」Requirement 落地、Decision 9 與 Create 階段 chrome-devtools-mcp 自我驗證契約完整落實；驗證 = 對虛擬 slug 在 MCP 可用時手動 walkthrough — skill 至少嘗試一次 exploit；在 MCP 不可用時 skill 顯示「請手動跑 pnpm challenge:verify <slug>」並不阻斷。
- [x] 7.2 [P] 在 `.agent/skills/wxl-creator/reference/agent-tools.md` 對 chrome-devtools-mcp 子集列出三家成熟度（Claude 完整 / Codex v0.20+ / Gemini 部分支援）並標示「自我驗證為 best-effort、不可用降級」；行為交付 = best-effort 降級的文件層說明、銜接 Decision 9 與「Host-agent-neutral prose 契約」；驗證 = grep 此檔應出現「chrome-devtools-mcp」、「best-effort」、三家名稱與成熟度註記。

## 8. Skill Mutate / Verify-on-Create 整合（Decision 3 + 4 + Skill prose 銜接）

- [x] 8.1 在 `.agent/skills/wxl-creator/SKILL.md` 新增 Mutate 階段章節：列觸發條件（使用者請求改 backend / difficulty / tags / category）、明確指示只呼 `pnpm challenge:retype <slug>` 與 `pnpm challenge:verify <slug>`、處理 exit code 2「manual retype required」時轉手給使用者；行為交付 = 「Skill supports the mutate stage via challenge:retype」Requirement 落地（Decision 3 prose 層）；驗證 = grep skill 應出現「pnpm challenge:retype」與「exit code 2」處理邏輯；手動 walkthrough 對 door-is-open 跑 difficulty 變更、跨語言變更兩情境。
- [x] 8.2 改寫 `.agent/skills/wxl-creator/SKILL.md` 內既有「Run analyze and validate」步驟為「Run pnpm challenge:verify <slug>」：依「Skill runs analyze and validate after creation」MODIFIED Requirement（header 名保留原樣，body 改為呼叫 layered verify）將 layered output 串接到 fix loop；行為交付 = 「Skill triggers challenge:verify automatically at the end of the Create flow」Requirement 落地、對應「Verify 階段在 Create 結束時的自動觸發契約」；驗證 = grep skill 應只出現「pnpm challenge:verify」（不再有「pnpm challenge:analyze && pnpm challenge:validate」這種舊指令組合）。
- [x] 8.3 改寫 `.agent/skills/wxl-creator/SKILL.md` 內既有 fix loop 章節：依「Skill auto-fixes validation errors with user confirmation」MODIFIED Requirement（header 名保留原樣，body 補強為 plain-text 提問），把所有 user confirmation 從 AskUserQuestion 改寫為 plain-text「apply / skip」prompt；行為交付 = 「Skill auto-fixes validation errors with user confirmation」Requirement 落地（Decision 2 在 fix loop 層的反映）；驗證 = grep 此章節應 0 個 `AskUserQuestion` match、出現「apply」/「skip」字串。

## 9. 既有題目 retrofit 與 regression（為 acceptance criteria 鋪路）

- [x] 9.1 為 `door-is-open` 補上 `tests/challenges/door-is-open.spec.ts`（若 6.3 已建立則確認 IDOR exploit path 對齊現 src 行為）並讓 `pnpm challenge:verify door-is-open` L1+L2+L3 全綠；行為交付 = canonical reference 在新 verify pipeline 下成立、為 acceptance criteria「對 door-is-open 跑 verify 全綠」鋪路；驗證 = 跑 `pnpm challenge:verify door-is-open` exit 0、stdout 末行 `verified: door-is-open (L1 L2 L3)`。
- [x] 9.2 跑全套 regression：`pnpm test` + `pnpm wasm:test` 都全綠，再對 door-is-open 跑 `pnpm challenge:verify door-is-open --blind`（前提：`pnpm docs:dev` 已啟動）並斷言 exit 0；行為交付 = 確認本變更不破壞既有測試、Decision 5 L4 端到端在 Claude runtime 跑通；驗證 = `pnpm test` exit 0、`pnpm wasm:test` exit 0、`pnpm challenge:verify door-is-open --blind` exit 0；任一 fail 阻擋 apply 完成。
- [x] 9.3 [P] 對 codex / gemini runtime 各跑一次 `WXL_VERIFY_RUNTIME=<rt> pnpm challenge:verify door-is-open --blind`，記錄結果（pass / inconclusive 都可接受）；行為交付 = 三家 runtime 在 L4 都至少能跑得起 spawn、確認 Decision 5 跨家覆蓋；驗證 = 兩次呼叫 exit code ∈ {0, 2}（exit 1 不可接受，代表抓到 wrong flag 應排查）；inconclusive 時要附 reason。

## 10. Self-review 與 spec drift 補強（落地後最後檢查）

- [x] 10.1 自動執行 host-agent-neutral grep check：`git grep -nE 'AskUserQuestion|EnterPlanMode|ExitPlanMode|TaskCreate|TaskUpdate|subagent_type' .agent/skills/wxl-creator/` 必須 exit 1；行為交付 = 「Skill prose is host-agent-neutral」與「Skill collects challenge parameters interactively」MODIFIED Requirement 末條 scenario 落地；驗證 = grep 退出碼 1（無匹配）；任何匹配阻擋 apply 完成。
- [x] 10.2 [P] 確認 `.agent/skills/wxl-creator/` 結構齊全：跑 `find .agent/skills/wxl-creator -type f | sort` 應印出至少 `SKILL.md / SKILL.zhTW.md / AGENTS.md / reference/agent-tools.md / reference/runtime-cli.md / templates/exploit-spec.ts.tmpl` 六個檔；行為交付 = Decision 1 與 Skill Cross-Agent Layout 契約結構完整；驗證 = `find` 結果含上述 6 個 path。
- [x] 10.3 [P] 確認 thin pointer 三家行為一致：`for p in .claude/skills/wxl-creator/SKILL.md .codex/skills/wxl-creator/SKILL.md .gemini/skills/wxl-creator/SKILL.md; do wc -l $p && grep -c '.agent/skills/wxl-creator/SKILL.md' $p; done`；每個檔行數 ≤ 8 且 grep 命中 ≥ 1；行為交付 = pointer 一致性符合「Skill is installed via a single source with thin pointer files」與其 example table；驗證 = 三個檔都通過上述條件。
- [x] 10.4 跑 `spectra validate wxl-creator-v2-cross-agent-pipeline` 與 `spectra analyze wxl-creator-v2-cross-agent-pipeline --json`，確認所有 Critical/Warning 已清乾淨或在 design 已說明 mitigation；行為交付 = 本 change 在 archive 前無未處理告警；驗證 = `spectra validate` exit 0；`spectra analyze` 的 JSON 內 `findings` 過濾 Critical/Warning 後為空陣列。

## 11. Design heading 對應索引（analyzer cross-reference + apply-time double-check）

本群每個 task 對應 design.md 內的一個 `###` 標題，要求 implementer 在落地對應行為後，**回頭比對 design 章節**確認契約欄位（Behavior / Interface / Failure modes / Acceptance criteria / Scope boundaries）全部落實。本群同時為 spectra analyzer 提供 design heading 字面字串。

- [x] 11.1 落地 design 章節「Decision 1: Skill 真實來源放 `.agent/skills/wxl-creator/`，三家用 thin pointer」後，回頭比對該 ### 標題下的 contract 欄位、確保 Task 2.1 / 2.6 / 10.2 / 10.3 全部 acceptance criteria 都覆蓋；驗證 = 對應四個 task 全綠且 `.agent/skills/wxl-creator/SKILL.md` 為唯一非 pointer 完整 prose 來源。
- [x] 11.2 落地 design 章節「Decision 2: 移除 `AskUserQuestion`、改成 plain-text 提問」後，回頭比對該 ### 標題下的 alternatives 與 acceptance criteria、確保 Task 2.2 / 8.3 / 10.1 全綠；驗證 = `git grep -nE 'AskUserQuestion' .agent/skills/wxl-creator/` 退出碼 1（無匹配）。
- [x] 11.3 落地 design 章節「Decision 3: Mutate 階段拆獨立 CLI（pnpm challenge:retype）而非 skill 內 Edit」後，回頭比對該 ### 標題下的 alternatives 與 Mutate CLI 契約對應的 Behavior 欄位、確保 Task 3.1 ~ 3.6 與 Task 8.1 全綠；驗證 = `pnpm challenge:retype --help` 列出全部四種 flag，且 skill prose 內無直接 Edit frontmatter 的指示。
- [x] 11.4 落地 design 章節「Decision 4: Verify pipeline 採 L1-L4 四層 gate」後，回頭比對該 ### 標題下的 alternatives 與 Verify CLI 契約 Acceptance criteria、確保 Task 4.1 ~ 4.8 全綠；驗證 = `pnpm challenge:verify door-is-open` 對應 stdout 應依序列 L1/L2/L3 三行 ✓ 結果。
- [x] 11.5 落地 design 章節「Decision 5: L4 採 spawn fresh agent CLI session、放棄 ctf-solver 風格 sub-agent」後，回頭比對該 ### 標題下的 Runtime CLI 對照、確保 Task 5.2 / 5.5 / 5.6 / 9.2 / 9.3 全綠；驗證 = 對 claude / codex / gemini 三家分別跑一次 L4、exit code ∈ {0, 2}（非 1）。
- [x] 11.6 落地 design 章節「Decision 6: Player-package 寫在 `tmp/wxl-verify/<slug>/`、verify 結束即刪、不引入 `solution/` 目錄」後，回頭比對該 ### 標題下的 alternatives 與 Ephemeral artefact cleanup 契約、確保 Task 5.1 / 5.8 / 1.2 全綠；驗證 = verify 結束後 `tmp/wxl-verify/door-is-open/` 不存在且 `docs/challenge/door-is-open/solution/` 不存在。
- [x] 11.7 落地 design 章節「Decision 7: L3 e2e 採 Playwright + `tests/challenges/<slug>.spec.ts`」後，回頭比對該 ### 標題下的 alternatives 與 spec template 契約、確保 Task 4.4 / 6.1 / 6.3 全綠；驗證 = `tests/challenges/door-is-open.spec.ts` 可被 `pnpm exec playwright test` 識別且 import `@playwright/test`。
- [x] 11.8 落地 design 章節「Decision 8: Harness 全程 TypeScript、不引入 Python」後，回頭比對該 ### 標題下的 alternatives、確保 Task 1.1 與全部 `scripts/challenge-*.ts` / `scripts/wxl-solver/*.ts` 都是純 TypeScript；驗證 = `find scripts -newer .gitignore -type f -name '*.py'` 回空、`requirements.txt` / `pyproject.toml` 不存在。
- [x] 11.9 落地 design 章節「Decision 9: Create 階段 chrome-devtools-mcp 自我驗證採 best-effort、MCP 不可用則優雅降級」後，回頭比對該 ### 標題下的 alternatives 與 Create 階段 chrome-devtools-mcp 自我驗證契約 Failure modes 欄位、確保 Task 7.1 / 7.2 全綠；驗證 = 模擬 MCP 不可用情境下 skill 走完 Create flow 且最終 stdout 出現「請手動跑 pnpm challenge:verify」提示。
- [x] 11.10 落地 design 章節「跨家 skill 安裝契約（Skill Cross-Agent Layout）」後，回頭比對該 ### 標題下的 file shape table、確保 Task 2.1 / 2.4 / 2.5 / 2.6 / 2.7 / 2.8 全綠；驗證 = `.agent/skills/wxl-creator/{SKILL.md, SKILL.zhTW.md, AGENTS.md, reference/agent-tools.md, reference/runtime-cli.md, templates/exploit-spec.ts.tmpl}` 六檔皆存在且三家 pointer 內容等價。
- [x] 11.11 落地 design 章節「Mutate CLI 契約（`scripts/challenge-retype.ts`）」後，回頭比對該 ### 標題下的 Exit code 表與 Acceptance criteria、確保 Task 3.1 ~ 3.6 與 Task 11.3 對齊；驗證 = Vitest 對 challenge-retype 三類錯誤 + 三類成功情境共 6 個 unit test 全綠。
- [x] 11.12 落地 design 章節「Verify CLI 契約（`scripts/challenge-verify.ts`）」後，回頭比對該 ### 標題下的 JSON shape 與 Exit code 規範、確保 Task 4.1 ~ 4.8 對齊；驗證 = `pnpm challenge:verify door-is-open --json` 輸出單一可解析 JSON、keys 完全符合 `slug,layers_run,results,summary,failed_at` 五欄。
- [x] 11.13 落地 design 章節「L4 Blind-solve 契約（`scripts/challenge-verify-blind.ts` + `scripts/wxl-solver/*.ts`）」後，回頭比對該 ### 標題下的 prompt template 五個必含子字串與 Runtime CLI 對照表、確保 Task 5.1 ~ 5.8 與 Task 11.5 對齊；驗證 = prompt unit test（5.7）+ runtime dispatch unit test（5.2）+ blind orchestration unit test（5.5）三個 Vitest 全綠。
- [x] 11.14 落地 design 章節「Create 階段 chrome-devtools-mcp 自我驗證契約（best-effort）」後，回頭比對該 ### 標題下的 Acceptance criteria（MCP 可用 vs 不可用兩情境）、確保 Task 7.1 / 7.2 / 11.9 對齊；驗證 = 兩個情境的手動 walkthrough 均通過且 skill 最終 exit 行為不阻斷整體 Create flow。
