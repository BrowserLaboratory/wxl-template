## 1. A01 reference document（落實「Pack ships an A01 reference document at the canonical path」、「Pack declares an A01 dispatch trigger regex」、「Pack reference document covers IDOR, JWT alg:none, and Path traversal primitives」與 design 決定 A01 guidance 落點：wxl-creator 子文件，不開新 skill；Reference 文件契約；新增 capability 而非僅 modify 既有 capability）

- [x] 1.1 撰寫 `.agent/skills/wxl-creator/reference/a01-access-control.md` 以滿足「Pack ships an A01 reference document at the canonical path」與「Pack reference document covers IDOR, JWT alg:none, and Path traversal primitives」，並落實 design Reference 文件契約（`.agent/skills/wxl-creator/reference/a01-access-control.md`）：含三節 `## Recognition heuristics`、`## Per-primitive fix hints`、`## Reference challenges table`，其中 `Per-primitive fix hints` 下含三個 `###` 子節 `IDOR`、`JWT alg:none`、`Path traversal`，各節提供「壞掉程式碼樣本 → 修補後樣本」具體建議；落點走 A01 guidance 落點：wxl-creator 子文件，不開新 skill 決定；驗證：`test -f` 該檔回 0、`grep -c '^## '` 回 3、`grep -c '^### '` ≥ 3、且 `grep -nE 'TBD|TODO|FIXME|\?\?\?|TKTK'` 無命中
- [x] 1.2 在文件 `Recognition heuristics` 節落實「Pack declares an A01 dispatch trigger regex」：寫死 trigger regex `/idor|jwt|path.?traversal|access.?control|broken.?access/i`，並依 A01 primitive 選擇：alg=none / classic `../` 決定列出涵蓋 primitive；驗證：`grep -F 'idor|jwt|path.?traversal|access.?control|broken.?access' .agent/skills/wxl-creator/reference/a01-access-control.md` 至少 1 命中
- [x] 1.3 在文件 `Reference challenges table` 節落實「Pack includes reference challenges covering every supported runtime」與 design「三題各擔一個 runtime（fastapi / flask / php）」決定：列出三題 slug / primitive / backend / difficulty 表格；驗證：表格包含 `door-is-open`、`jwt-none-alg`、`confidential-files` 三 row 與 `fastapi`、`flask`、`php` 三 backend
- [x] 1.4 對新增 A01 reference 文件跑禁字掃描以避免 R1 catch-22：`git grep -nE 'AskUserQuestion|Agent\(subagent_type|EnterPlanMode|ExitPlanMode|TaskCreate|TaskUpdate' .agent/skills/wxl-creator/reference/a01-access-control.md` 必 exit 1；驗證：上述命令確實 exit 1（0 命中）

## 2. wxl-creator skill 雙語 registry table 與 fix-loop 擴充（落實「Skill consumes capability-specific reference documents via a registry table」與 design 決定 wxl-creator-skill 用「通用 registry table」延伸點，而非「a01-specific requirement」；wxl-creator SKILL.md / SKILL.zhTW.md 變更契約）

- [x] 2.1 `[P]` 在 `.agent/skills/wxl-creator/SKILL.md` 落實「Skill consumes capability-specific reference documents via a registry table」：Workflow Step 3「Generate vulnerable application code」前插入 step 3.0「Consult capability-specific reference (if applicable)」並新增 Markdown registry table（欄位 `Trigger regex` / `Reference file`），首 row 對應 A01；Step 8.c「Propose fixes」append prose 規範「若失敗 challenge 的 `tags` 與已知 capability 的 taxonomy 相交，諮詢對應 `reference/<capability>.md` 的 Per-primitive fix hints」；此延伸機制依 wxl-creator-skill 用「通用 registry table」延伸點，而非「a01-specific requirement」決定；驗證：`grep -n '3\.0' SKILL.md` 至少 1 命中、registry table 含 `reference/a01-access-control.md` row、Step 8.c 含 `Per-primitive fix hints` 字串
- [x] 2.2 `[P]` 對 `.agent/skills/wxl-creator/SKILL.zhTW.md` 做雙語對等同步以維持 wxl-creator SKILL.md / SKILL.zhTW.md 變更契約 行為一致：與 2.1 同行為，但 prose 為台灣繁體中文；registry table 條目與 SKILL.md 完全一致；驗證：兩檔 registry table row 集合相等（`diff <(grep -A20 'Trigger regex' SKILL.md | grep -oE 'reference/[a-z0-9-]+\.md') <(grep -A20 'Trigger regex' SKILL.zhTW.md | grep -oE 'reference/[a-z0-9-]+\.md')` 無差異）
- [x] 2.3 跑 wxl-creator skill 全目錄禁字掃描（catch-22 預防、落實 wxl-creator SKILL.md / SKILL.zhTW.md 變更契約 的 host-agent-neutral 要求）：`git grep -nE 'AskUserQuestion|Agent\(subagent_type|EnterPlanMode|ExitPlanMode|TaskCreate|TaskUpdate' .agent/skills/wxl-creator/` 必 exit 1；驗證：命令 exit 1

## 3. 新 reference challenge `jwt-none-alg`（Flask + pyjwt，落實「Pack includes reference challenges covering every supported runtime」Flask coverage、「Pack declares an A01 tag taxonomy」對應 row，並 dogfood design 決定 三題各擔一個 runtime（fastapi / flask / php）與 A01 primitive 選擇：alg=none / classic `../`；同時建立 新 reference challenge 程式碼契約 的 Flask first production reference）

- [x] 3.1 `[P]` 透過 `pnpm create:challenge --name jwt-none-alg --backend flask --difficulty medium` scaffold 一題以實踐 三題各擔一個 runtime（fastapi / flask / php）中的 Flask coverage 並建立 新 reference challenge 程式碼契約 的 Flask 部分；驗證：`docs/challenge/jwt-none-alg/` 目錄與 `index.md`、`src/app.py`、`src/flag.txt` 存在
- [x] 3.2 實作 `docs/challenge/jwt-none-alg/src/app.py` 以落實 A01 primitive 選擇：alg=none / classic `../` 的 alg=none 分支與 新 reference challenge 程式碼契約 的 Flask 部分：Flask app、`POST /login` 簽發 HS256 JWT（含 `sub`、`role`）、`GET /admin` 用 `jwt.decode(token, key, algorithms=['HS256', 'none'])` 接受 alg=none token → 偽造 `{"alg":"none"}` header + `{"role":"admin"}` payload 繞過驗證；程式碼結構參照 `.archive/challenge/sqli-demo/src/app.py`（路由、設定、回應）但換 vulnerability；驗證：`python -c "import ast; ast.parse(open('docs/challenge/jwt-none-alg/src/app.py').read())"` 無 SyntaxError、檔內含字串 `algorithms` 與 `'none'`
- [x] 3.3 更新 `docs/challenge/jwt-none-alg/index.md` frontmatter 以落實「Pack declares an A01 tag taxonomy」對 jwt-none-alg 之要求並依 A01 tag taxonomy：free-text `tags` + 取交集判斷 設計：`backend: flask`、`difficulty: medium`、`packages: [pyjwt]`、`tags: [jwt, access-control, authentication, flask]`、`description` 為一句場景敘事；驗證：YAML 解析後 `backend == "flask"`、`packages == ["pyjwt"]`、`tags` 與 A01 taxonomy `{idor, access-control, jwt, path-traversal, broken-access}` 相交非空
- [x] 3.4 `[P]` 透過 `templates/exploit-spec.ts.tmpl` 衍生 `tests/challenges/jwt-none-alg.spec.ts` 以驗證 新 reference challenge 程式碼契約：偽造 alg=none token、GET `/admin`、斷言 response 含 `FLAG{`；驗證：spec 檔存在且能被 playwright 解析（`pnpm test:e2e --list | grep jwt-none-alg` 至少 1 命中）

## 4. 新 reference challenge `confidential-files`（PHP + LFI，落實「Pack includes reference challenges covering every supported runtime」PHP coverage、「Pack declares an A01 tag taxonomy」對應 row，並 dogfood design 決定 三題各擔一個 runtime（fastapi / flask / php）與 A01 primitive 選擇：alg=none / classic `../`；同時建立 新 reference challenge 程式碼契約 的 PHP first production reference）

- [x] 4.1 `[P]` 透過 `pnpm create:challenge --name confidential-files --backend php --difficulty easy` scaffold 一題以實踐 三題各擔一個 runtime（fastapi / flask / php）中的 PHP coverage 並建立 新 reference challenge 程式碼契約 的 PHP 部分；驗證：`docs/challenge/confidential-files/` 目錄與 `index.md`、`src/index.php`（PHP backend scaffold 預設）、`src/flag.txt` 存在
- [x] 4.2 實作 `docs/challenge/confidential-files/src/index.php` 以落實 A01 primitive 選擇：alg=none / classic `../` 的 path traversal 分支與 新 reference challenge 程式碼契約 的 PHP 部分：列出 `reports/` 公開報告、`view.php?file=<name>` endpoint 用 `file_get_contents("reports/" . $_GET['file'])` 無 canonicalize → 可 `../`；程式碼結構參照 `.archive/challenge/php-demo/src/index.php` 但換 vulnerability；驗證：`php -l docs/challenge/confidential-files/src/index.php` 回 `No syntax errors detected`
- [x] 4.3 更新 `docs/challenge/confidential-files/index.md` frontmatter 以落實「Pack declares an A01 tag taxonomy」對 confidential-files 之要求並依 A01 tag taxonomy：free-text `tags` + 取交集判斷 設計：`backend: php`、`difficulty: easy`、`packages: []`、`tags: [path-traversal, access-control, lfi, php]`、`description` 為一句場景敘事；驗證：YAML 解析後 `backend == "php"`、`tags` 與 A01 taxonomy 相交含 `{path-traversal, access-control}`
- [x] 4.4 `[P]` 透過 `templates/exploit-spec.ts.tmpl` 衍生 `tests/challenges/confidential-files.spec.ts` 以驗證 新 reference challenge 程式碼契約：mutate `?file=../flag.txt`、斷言 response body 含 `FLAG{`；驗證：spec 檔存在且能被 playwright 解析（`pnpm test:e2e --list | grep confidential-files` 至少 1 命中）

## 5. 三題端到端驗證與 spec 驗證（落實 design「失敗模式與接受標準」、「觀察行為（apply 完成後）」與 範圍邊界（避免 apply 漂移）；確認 R8 未越出範圍邊界（避免 apply 漂移）所列 out-of-scope 項目）

- [x] 5.1 跑三題 layered verify 以驗證 design「觀察行為（apply 完成後）」並守住 範圍邊界（避免 apply 漂移）（door-is-open 不可被誤改）：`pnpm challenge:verify door-is-open && pnpm challenge:verify jwt-none-alg && pnpm challenge:verify confidential-files`；驗證：三命令皆 exit 0（door-is-open 為 regression check、確認 R8 未誤改既有題）。**已驗（2026-05-31）**：L1（validate）+ L2（keygen + wasm-tools validate）三題全綠。L3 端到端因本機 port 5173 被別專案 `open-slide` 長駐、且 default config 30s timeout 太短 → 改在 wxl 獨佔 **port 5178** + `--timeout=120000` 跑（specs 暫時 redirect、跑完還原成 committed 5173）：**三題全 PASS（PW_EXIT=0）**——door-is-open(IDOR/FastAPI) 4.6s、jwt-none-alg(alg=none/Flask) 4.5s、confidential-files(LFI/PHP) 5.2s。L3 過程抓到並修掉兩個真 bug：(1) confidential-files：php-wasm 絕對路徑 `/flag.txt` 讀不到→改相對路徑（base dir `reports` 而非 `/reports`）；(2) jwt-none-alg：app code 結尾的 `if __name__=='__main__': app.run(...)` 在 Pyodide 執行時去 bind socket → `OSError [Errno 138] Not supported` → `SystemExit` → runtime 的 `_asgi_bridge` 未定義 → handleRequest `bridge is not a function`（debug spec traceback 實證；root cause 非 pyjwt）。修法：刪除 `app.run()` 區塊、JWT 改用 Python stdlib（hmac/hashlib/base64/json）self-contained 實作、`packages: []`。door-is-open regression 未被破壞。`pnpm challenge:verify`（default 30s/5173）需在 CI 或釋放 5173 的乾淨環境才會原生 exit 0。
- [x] 5.2 跑 spec 驗證以閉合 design「失敗模式與接受標準」：`spectra validate a01-access-control-template-pack`；驗證：命令 exit 0、輸出無 Critical/Warning finding
