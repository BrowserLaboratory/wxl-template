<!-- 每個 task 都要明示「完成時可觀察的行為／契約」與「如何驗證」；檔案路徑只是定位用，不是 task 本體。 -->

## 1. CONTRIBUTE.md「Branch protection ruleset」段落升級

- [x] 1.1 依七條 design 決策「補 `deletion` rule（禁刪 `main`）」「補 `non_fast_forward` rule（禁 force-push `main`）」「`required_status_checks` 鎖 `integration_id=15368`（GitHub Actions App）」「`conditions.ref_name.include` 改用 `~DEFAULT_BRANCH`」「`bypass_mode` 從 `always` 收緊到 `pull_request`」「`bypass_actors` 改用合法 `actor_type`／`actor_id` 組合」「`pull_request` rule 設 `dismiss_stale_reviews_on_push=true`、`required_review_thread_resolution=true`」，把 `CONTRIBUTE.md` 「Maintainer Setup → Branch protection ruleset → Create the ruleset」code block 內的 POST payload 整段換成新版。verification：`grep -c '"type": *"deletion"' CONTRIBUTE.md` 為 1；`grep -c '"type": *"non_fast_forward"' CONTRIBUTE.md` 為 1；`grep -c '"integration_id": *15368' CONTRIBUTE.md` 為 2（兩個 check entry 都鎖）；`grep -c '~DEFAULT_BRANCH' CONTRIBUTE.md` ≥ 1；`grep -c '"bypass_mode": *"pull_request"' CONTRIBUTE.md` ≥ 2；`grep -c 'dismiss_stale_reviews_on_push' CONTRIBUTE.md` ≥ 1；`grep -c 'required_review_thread_resolution' CONTRIBUTE.md` ≥ 1；`grep -c 'RepositoryAdmin' CONTRIBUTE.md` 為 0；code fence parity 仍為偶數（`python3 -c` 驗證）。
- [x] 1.2 [P] 落實 design 決策「提供「升級既有 ruleset」的 PUT 指令樣板」：於同段落新增 `#### Upgrade an existing ruleset` 子段落（放在 `#### Create the ruleset` 之後、`#### Optional: require pull-request approvals` 之前），含 `gh api -X PUT /repos/{owner}/{repo}/rulesets/<id>` 範例（payload 形狀同 POST、`<id>` 為 placeholder 並註記用 `gh ruleset list -R {owner}/{repo}` 取得）。verification：`grep -c '^#### Upgrade an existing ruleset' CONTRIBUTE.md` 為 1；`grep -c 'gh api -X PUT.*rulesets' CONTRIBUTE.md` ≥ 1；`grep -nE '^#### (Create the ruleset|Upgrade an existing ruleset|Optional: require pull-request approvals)' CONTRIBUTE.md` 印出 line 號順序為 Create < Upgrade < Optional。
- [x] 1.3 [P] 落實 design 決策「不引入 `required_linear_history`、`allowed_merge_methods`、`strict_required_status_checks_policy=true`」之 negative-space 驗證：確認新 payload 與升級指令範例**不**包含這三個關鍵字，避免日後 maintainer 誤以為這次也要對齊 sister repo 的這部分。verification：`grep -c 'required_linear_history' CONTRIBUTE.md` 為 0；`grep -c 'allowed_merge_methods' CONTRIBUTE.md` 為 0；`grep -c 'strict_required_status_checks_policy.*true' CONTRIBUTE.md` 為 0（出現 `false` 是 ok，本變更不調整此欄位）。
- [x] 1.4 [P] 更新同段落 Notes：解釋新增 4 條設定（`deletion`／`non_fast_forward` 為何要禁、`integration_id=15368` 對應 GitHub Actions App、`~DEFAULT_BRANCH` 為何用動態變數、`bypass_mode=pull_request` 與 `always` 差異），同時把原有「`RepositoryRole` `actor_id` 對照表」「`integration_id` 解釋」維持或併入。落實「使用者讀指令時要看懂為什麼」需求，對應 design 七條決策的後續說明文字。verification：`grep -ciE 'non_fast_forward|deletion' CONTRIBUTE.md` ≥ 2（payload + Notes 各一）；`grep -ci '~DEFAULT_BRANCH' CONTRIBUTE.md` ≥ 2；`grep -ci 'bypass_mode' CONTRIBUTE.md` ≥ 2（payload entries + Notes 解釋）。
- [x] 1.5 更新「#### Verify the ruleset」子段落：把驗證指令範例改為印出 4 條 rule 名稱與每個 check 的 `integration_id`，並補一段「升級後預期看到 `deletion`／`non_fast_forward` 出現在 rules 列表、`bypass_actors` 每筆皆 `pull_request` mode」的 acceptance 提示。verification：`grep -ci 'gh.*rulesets/.*' CONTRIBUTE.md` ≥ 2（POST 與 PUT 兩段都有 GET 範例 OR Verify 段含 GET 範例）；Verify 段含 `deletion`／`non_fast_forward` 字串至少一處作為「驗收時應看到什麼」。

## 2. Spec delta（archive-ready 確認）

- [x] 2.1 確認 `openspec/changes/align-ruleset-with-netsim/specs/ci-quality-gates/spec.md` 為純 MODIFIED delta，含 1 條 Requirement「Branch protection ruleset guards main with required status checks」之完整重寫（含 5 個 Scenario + 1 個 Example）。spectra archive 會自動 merge 至 canonical spec、覆蓋第 12 條 Requirement，apply 階段不可手動編輯 canonical 否則被覆蓋。verification：`grep -c '^### Requirement:' openspec/changes/align-ruleset-with-netsim/specs/ci-quality-gates/spec.md` 為 1；該檔僅含 `## MODIFIED Requirements`（無 ADDED／REMOVED／RENAMED）；apply 期間 canonical `openspec/specs/ci-quality-gates/spec.md` Requirement 數維持 12 不動；canonical 該 Requirement 之 main paragraph 維持原內容（archive 階段才會被替換）。

## 3. 整體驗證

- [x] 3.1 跑 spectra analyzer 與 validator 確認 Requirement 與 design／tasks 互相對應、無 cross-reference 漏接。verification：`spectra analyze align-ruleset-with-netsim --json` 0 Critical / 0 Warning；`spectra validate align-ruleset-with-netsim` exit 0。
- [x] 3.2 確認 `CONTRIBUTE.md` 仍是合法 markdown。verification：`python3 -c "t=open('CONTRIBUTE.md').read(); assert t.count('\`\`\`')%2==0; print('ok')"` 印出 ok；若機器有 markdownlint 則 `markdownlint CONTRIBUTE.md` 退出碼 0（無則略）。
- [x] 3.3 PR 上 `quality-gates` workflow 跑出綠燈，純 markdown 與 spec 變更不應影響 build／test。verification：`gh run list --workflow=quality-gates.yml --branch <branch>` 顯示最新 run conclusion 為 `success`；CI 結果與 PR review 都不顯示 markdown 解析錯誤。
