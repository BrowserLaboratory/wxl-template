<!-- 每個 task 都要明示「完成時可觀察的行為／契約」與「如何驗證」；檔案路徑只是定位用，不是 task 本體。 -->

## 1. CONTRIBUTE.md「Maintainer Setup」段落

- [x] 1.1 依「CONTRIBUTE.md 新章節放尾端」決策，於 `CONTRIBUTE.md` 既有「Reporting issues」段落**之後**新增 top-level section `## Maintainer Setup`，並落實「CONTRIBUTE documents maintainer setup for branch protection ruleset」的「Contributor skipping maintainer section」scenario——section 首句明示「This section is for repository maintainers only; contributors can skip it」。verification：`grep -n '^## Maintainer Setup' CONTRIBUTE.md` 印出剛好 1 行；該行 line 號大於 `grep -n '^## Reporting issues' CONTRIBUTE.md` 印出的 line 號；首段含 `maintainers only` 字串。
- [x] 1.2 於「Maintainer Setup」下新增 `### Branch protection ruleset` 子段落，依「Ruleset，不用 legacy Branch protection rules」、「`gh` 指令用 `gh api` 而非 `gh ruleset create`」、「Required status checks 只含 `test`／`build`」、「設「Require a pull request before merging」，allow admin bypass」四條決策提供：(a) why-this-matters 段落並引用 `ci-quality-gates` 的「Branch protection ruleset guards main with required status checks」Requirement、(b) 可貼上執行的 `gh api -X POST /repos/{owner}/{repo}/rulesets` bash code block，payload 含 `name=Protect main`、`target=branch`、`enforcement=active`、`conditions.ref_name.include=[refs/heads/main]`、`bypass_actors` 含 OrganizationAdmin／RepositoryAdmin、`rules` 含 pull_request type 與 required_status_checks type（contexts 為 `test` 與 `build`）、(c) `gh ruleset list` 與 `gh api -X GET /repos/{owner}/{repo}/rulesets/{id}` 驗證指令。verification：`grep -c '^### Branch protection ruleset$' CONTRIBUTE.md` 為 1；`grep 'gh api.*rulesets' CONTRIBUTE.md` 至少 1 行；`grep -E '"context": *"(test|build)"' CONTRIBUTE.md` 顯示 2 行；`grep -i 'OrganizationAdmin\|RepositoryAdmin' CONTRIBUTE.md` 至少 1 行。
- [ ] 1.3 依「Approval count = 0（default），文件指引升級到 1」決策，於 `### Branch protection ruleset` 子段落內、default `gh api` 指令之後，新增 approval policy 升級指引片段，落實「Team scaling to multiple reviewers」scenario：明示「If the team has multiple reviewers」並提供差異片段（在 `pull_request` rule parameters 加 `required_approving_review_count: 1`），保留既有 required status checks 不被換掉。verification：`grep -i 'required_approving_review_count' CONTRIBUTE.md` 至少 1 行；該關鍵字所在 line 號大於 default `gh api -X POST` 指令所在 line 號。

## 2. Spec delta（archive-ready 確認）

- [ ] 2.1 [P] 確認 `openspec/changes/configure-branch-protection-ruleset/specs/ci-quality-gates/spec.md` 為純 ADDED delta，含 1 條 Requirement「Branch protection ruleset guards main with required status checks」（3 個 Scenario + 1 個 Example）。spectra archive 會自動 merge 至 canonical spec，apply 階段不可手動 append 否則重複。verification：`grep -c '^### Requirement:' openspec/changes/configure-branch-protection-ruleset/specs/ci-quality-gates/spec.md` 為 1；該檔僅含 `## ADDED Requirements`（無 MODIFIED／REMOVED／RENAMED）；apply 期間 canonical `openspec/specs/ci-quality-gates/spec.md` Requirement 數維持 11 不動。
- [ ] 2.2 [P] 確認 `openspec/changes/configure-branch-protection-ruleset/specs/contributor-guide/spec.md` 為純 ADDED delta，含 1 條 Requirement「CONTRIBUTE documents maintainer setup for branch protection ruleset」（3 個 Scenario）。verification：`grep -c '^### Requirement:' openspec/changes/configure-branch-protection-ruleset/specs/contributor-guide/spec.md` 為 1；該檔僅含 `## ADDED Requirements`；apply 期間 canonical `openspec/specs/contributor-guide/spec.md` Requirement 數維持 5 不動。

## 3. 整體驗證

- [ ] 3.1 跑 spectra analyzer 確認兩條 Requirement 與 design／tasks 互相對應、無 cross-reference 漏接。verification：`spectra analyze configure-branch-protection-ruleset --json` 0 Critical / 0 Warning；`spectra validate configure-branch-protection-ruleset` exit 0。
- [ ] 3.2 確認 `CONTRIBUTE.md` 仍是合法 markdown（無未閉合 code fence、無 broken table）。verification：本機跑 `python3 -c "t=open('CONTRIBUTE.md').read(); assert t.count('\`\`\`')%2==0; print('ok')"` 印出 ok；若機器有 markdownlint 則 `markdownlint CONTRIBUTE.md` 退出碼 0（無則略）。
- [ ] 3.3 PR 上 `quality-gates` workflow 跑出綠燈，純 markdown 與 spec 變更不應影響 build／test。verification：`gh run list --workflow=quality-gates.yml --branch <branch>` 顯示最新 run conclusion 為 `success`；CI 結果與 PR review 都不顯示 markdown 解析錯誤。
