<!-- 每個 task 都要明示「完成時可觀察的行為／契約」與「如何驗證」；檔案路徑只是定位用，不是 task 本體。 -->

## 1. CONTRIBUTE.md「Branch protection ruleset」段落升級

- [ ] 1.1 依七條 design 決策「採用 `required_linear_history` rule」「採用 `allowed_merge_methods: ["squash", "rebase"]` parameter」「採用 `strict_required_status_checks_policy: true`」「三條紅燈同時採用、不分批」「不調整 ruleset id `16637478` 之 ownership 或 conditions」，把 `CONTRIBUTE.md` 「Maintainer Setup → Branch protection ruleset → Create the ruleset」code block 內 POST payload 之 `rules` array 新增 `{ "type": "required_linear_history" }`、`pull_request.parameters` 內新增 `"allowed_merge_methods": ["squash", "rebase"]`、`required_status_checks.parameters.strict_required_status_checks_policy` 由 `false` 改 `true`。verification：`grep -c '"type": *"required_linear_history"' CONTRIBUTE.md` ≥ 1；`grep -c '"allowed_merge_methods": *\["squash", *"rebase"\]' CONTRIBUTE.md` ≥ 1；`grep -c '"strict_required_status_checks_policy": *true' CONTRIBUTE.md` ≥ 2（POST 段一處、PUT 段一處）；`grep -c '"strict_required_status_checks_policy": *false' CONTRIBUTE.md` 為 0；code fence parity 仍為偶數（`python3 -c "t=open('CONTRIBUTE.md').read(); assert t.count('\`\`\`')%2==0; print('ok')"` 印 ok）。
- [ ] 1.2 [P] 把同樣三條修改同步到 `#### Upgrade an existing ruleset` 副段落內 PUT payload，使 POST／PUT 兩段 payload 形狀完全一致。verification：`diff <(awk '/^```bash$/,/^```$/' CONTRIBUTE.md | sed -n '/POST/,/^JSON$/p') <(awk '/^```bash$/,/^```$/' CONTRIBUTE.md | sed -n '/PUT/,/^JSON$/p')` 結果除 HTTP verb 與 URL 之外無 payload 差異；POST 段與 PUT 段各自含一份 `"type": "required_linear_history"` 與 `"allowed_merge_methods"` 與 `"strict_required_status_checks_policy": true`。
- [ ] 1.3 [P] 落實 design 決策「Notes 段補三條新設定之解釋」：在「Create the ruleset」code block 後 Notes bullet list 新增三段，分別解釋 (a) `required_linear_history` 之效果與對 git log 樣貌影響、(b) `allowed_merge_methods: ["squash","rebase"]` 是 UI 強制（merge commit 按鈕不可用）且與 `required_linear_history` 為 belt-and-suspenders、(c) `strict_required_status_checks_policy: true` 與「綠了又被打回」副作用之解釋，並各自註明對 solo／多人團隊／衍生 repo 之差異。verification：`grep -ci 'required_linear_history' CONTRIBUTE.md` ≥ 3（POST payload + PUT payload + Notes）；`grep -ci 'allowed_merge_methods' CONTRIBUTE.md` ≥ 3；`grep -ci 'strict_required_status_checks_policy' CONTRIBUTE.md` ≥ 3；Notes 段明文出現「綠了又被打回」或同義英文片語（「out of date with base」「stale base」之一），由 spectra archive 之後人工 review 確認。
- [ ] 1.4 [P] 更新「#### Verify the ruleset」副段落：把 Python 片段預期 `rule types` 從 4 條（`deletion`／`non_fast_forward`／`pull_request`／`required_status_checks`）改成 5 條（多 `required_linear_history`）；補一段 Python 片段印出 `pull_request.parameters.allowed_merge_methods` 與 `required_status_checks.parameters.strict_required_status_checks_policy` 之值；Expected output 對應更新。verification：`grep -c "rule types: \['deletion', 'non_fast_forward', 'pull_request', 'required_linear_history', 'required_status_checks'\]" CONTRIBUTE.md` ≥ 1（或同等 sorted 順序之 expected output）；`grep -ci 'allowed_merge_methods.*squash.*rebase' CONTRIBUTE.md` ≥ 1（Verify 段預期輸出）；`grep -ci 'strict_required_status_checks_policy.*True' CONTRIBUTE.md` ≥ 1（Verify 段預期輸出，Python repr 為 `True`）。
- [ ] 1.5 [P] 確認「Optional: require pull-request approvals (multi-reviewer teams)」副段落之 pull_request rule 範例也帶 `allowed_merge_methods` parameter（不然依 GitHub API all-or-nothing 規則會 422）。verification：`grep -c '"allowed_merge_methods"' CONTRIBUTE.md` ≥ 3（Create POST + Upgrade PUT + Optional multi-reviewer 範例，各一處）。

## 2. Spec delta（archive-ready 確認）

- [ ] 2.1 確認 `openspec/changes/wxl-ruleset-strict-hardening/specs/ci-quality-gates/spec.md` 為純 MODIFIED delta，含 1 條 Requirement「Branch protection ruleset guards main with required status checks」之**完整重寫**（Requirement header 與現行 canonical 一致、body 含三條新規範描述、Scenarios 為原 5 個 + 新 3 個 = 8 個 + 1 個 Example）。spectra archive 會 merge 至 canonical spec、覆蓋第 12 條 Requirement，apply 階段不可手動編輯 canonical 否則被覆蓋。verification：`grep -c '^### Requirement:' openspec/changes/wxl-ruleset-strict-hardening/specs/ci-quality-gates/spec.md` 為 1；該檔僅含 `## MODIFIED Requirements`（無 ADDED／REMOVED／RENAMED）；apply 期間 canonical `openspec/specs/ci-quality-gates/spec.md` Requirement 數維持 12 不動；archive 後 canonical 該 Requirement 之 body 應反映三條新 hardening。
- [ ] 2.2 Header 一致性驗證：spec delta 之 Requirement header 必須與 canonical 第 12 條 Requirement header **逐字一致**（避開「MODIFIED Requirement header 變更被 spectra archive 半自動跳過」之 feedback memory 陷阱）。verification：`diff <(grep '^### Requirement:' openspec/changes/wxl-ruleset-strict-hardening/specs/ci-quality-gates/spec.md) <(sed -n '273p' openspec/specs/ci-quality-gates/spec.md)` 無差異。

## 3. 整體驗證

- [ ] 3.1 跑 spectra analyzer 與 validator 確認 Requirement 與 design／tasks 互相對應、無 cross-reference 漏接。verification：`spectra analyze wxl-ruleset-strict-hardening --json` 0 Critical / 0 Warning；`spectra validate wxl-ruleset-strict-hardening --strict` exit 0。
- [ ] 3.2 確認 `CONTRIBUTE.md` 仍是合法 markdown。verification：`python3 -c "t=open('CONTRIBUTE.md').read(); assert t.count('\`\`\`')%2==0; print('ok')"` 印出 ok；若機器有 markdownlint 則 `markdownlint CONTRIBUTE.md` 退出碼 0（無則略）。
- [ ] 3.3 本機 wxl repo 自我測試（在 maintainer 跑 PUT 升級 ruleset 之前）：用 `gh api -X GET /repos/BrowserLaboratory/wxl-template/rulesets/16637478` 抓出**目前** server-side 狀態，跟新 payload diff，確認改動點完全與本 change 描述對齊、沒有意外漂移其他欄位。verification：人工 review diff 結果；除 `rules` 多 1 條、`pull_request.parameters` 多 1 個 key、`required_status_checks.parameters.strict_required_status_checks_policy` 值由 `false` 變 `true` 之外無其他變化。
- [ ] 3.4 PUT 動作預演（dry-run、不實際發送）：在本地把新 payload 寫入 `/tmp/ruleset-upgrade.json`，用 `jq` 驗證 JSON 合法、用 `python3 -m json.tool` 二次驗證，再用 `gh api --method PUT /repos/.../rulesets/16637478 --input /tmp/ruleset-upgrade.json` **不要按下 Enter**（或加 `--dry-run` 如有支援；目前 `gh api` 無 dry-run，採用「複製到剪貼簿但不執行」之手動 dry-run）。verification：`jq . /tmp/ruleset-upgrade.json` 退出碼 0；`python3 -m json.tool /tmp/ruleset-upgrade.json` 退出碼 0；payload 與 CONTRIBUTE.md PUT 段落內容逐字一致。
- [ ] 3.5 PR 上 `quality-gates` workflow 跑出綠燈，純 markdown 與 spec 變更不應影響 build／test。verification：`gh run list --workflow=quality-gates.yml --branch feature/wxl-ruleset-strict-hardening` 顯示最新 run conclusion 為 `success`；CI 結果與 PR review 都不顯示 markdown 解析錯誤。
- [ ] 3.6 文件 framing 一致性：spec 與 CONTRIBUTE.md 內**不得**出現「對齊 sister repo」「align with sister」「複製 netsim ruleset payload」之描述（防止誤導未來 maintainer 以為這次也是從 sister 抄）。verification：`grep -ci 'sister' openspec/changes/wxl-ruleset-strict-hardening/ CONTRIBUTE.md` 為 0；`grep -ci 'netsim' openspec/changes/wxl-ruleset-strict-hardening/specs/` 為 0；`grep -ci 'align.*ruleset' openspec/changes/wxl-ruleset-strict-hardening/specs/` 為 0（proposal.md / design.md 內可提及 align-ruleset-with-netsim 作為「前一個 change」之歷史背景、屬於正常引用，由 spectra validate 時人工 review）。

## 4. Open Questions 處理（PR review 期間決議）

- [ ] 4.1 與團隊（或 solo 自評）討論並決議：是否要先在 staging branch 試一週後再 promote 三條紅燈到 main？相關討論記錄於本 change `design.md` Open Questions 段，PR review 期間於 PR comment 留下決議結論；若決議改採「先 staging 一週」路徑，本 change 之 design 與 CONTRIBUTE.md 需相應調整（staging 路徑屬於 `staging` branch ruleset 另開 change 之 prerequisite）。verification：PR comment 或 issue 留下 yes／no 決議；若 yes，建立後續 staging branch ruleset change ticket；若 no，於 design.md Open Questions 標註「已決議：直接合入 main、不走 staging」。
- [ ] 4.2 與團隊（或 solo 自評）決議：是否要分批合入三條紅燈（例如先合 `required_linear_history` + `allowed_merge_methods`、觀察一週後再合 `strict_required_status_checks_policy`），以隔離「綠了又被打回」副作用之觀察？verification：PR comment 留下 yes／no 決議；若 yes，本 change 須拆為兩 change（例如 `-history-only` 與 `-strict-checks-only`）；若 no，於 design.md Open Questions 標註「已決議：三條同時合入」。
