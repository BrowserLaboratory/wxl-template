<!--
每個任務描述同時記錄「交付的行為 / 契約」與「驗證方式」。檔案路徑為定位用脈絡，不是任務本身。
[P] = parallel-eligible（不同檔案、不與同群組未完成任務有依賴）。
所有 stage 結束以 `/spectra-audit` + `/tw-emoji-commit` 收尾，回應設計決策「五階段切分搭配雙層稽核 gate」。
-->

## 1. 建立 zh-TW challenge 樹並完成 door-is-open 雙語 body 對位

實作設計決策「Challenge 雙語化採「資料層解決、layout 不動」」與「zh-TW challenge 樹不複製 `src/` 與 `runtime.wasm`」，並落地需求「Challenge frontmatter values are localized per content tree」。

- [x] 1.1 建立 `docs/zh-TW/challenge/door-is-open/index.md`，frontmatter 之 `difficulty`、`category`、`backend`、`app`、`packages`、`tools`、`source_visible`、`date`、`tags`、`wasmModule` 逐字等於英文版 `docs/challenge/door-is-open/index.md`；`title` 為繁中 `門已敞開`、`description` 為繁中 IDOR 描述；body 為對應繁中題目敘述（取自 git history 之前繁中版）。交付行為：「Challenge frontmatter values are localized per content tree」之 invariant 在 door-is-open slug 達成（技術欄位逐字一致、僅 `title`/`description`/body 語言不同）。Verification：`diff <(rg -N 'difficulty:\|category:\|backend:\|app:\|packages:\|tools:\|source_visible:\|date:\|tags:\|wasmModule:' docs/challenge/door-is-open/index.md) <(rg -N 'difficulty:\|category:\|backend:\|app:\|packages:\|tools:\|source_visible:\|date:\|tags:\|wasmModule:' docs/zh-TW/challenge/door-is-open/index.md)` 顯示兩檔之技術欄位 byte-identical；`rg '門已敞開' docs/zh-TW/challenge/door-is-open/index.md` 命中 frontmatter title 行；`pnpm docs:build` 後 `dist/zh-TW/challenge/door-is-open/index.html` 存在且 `<html lang="zh-TW">`。
- [x] 1.2 改寫 `docs/challenge/door-is-open/index.md` body 為英文（題目敘述、目標、提示）；frontmatter 完全不動。交付行為：英文路由 `/challenge/door-is-open/` 之題目敘述以英文呈現，與既有英文 frontmatter `title: Door Is Open` 一致。Verification：`rg '[一-鿿]' docs/challenge/door-is-open/index.md` exit 1（frontmatter 已英文、body 改後 0 CJK hits）；`pnpm docs:build` 後 `dist/challenge/door-is-open/index.html` 內無 CJK 字元。
- [x] 1.3 Stage 1 收尾：依「五階段切分搭配雙層稽核 gate」執行 `/spectra-audit`，Critical+High = 0 後以 `/tw-emoji-commit` 落 commit。交付行為：stage 1 之檔案變更達成穩定檢查點。Verification：`spectra analyze content-i18n-migration --json` Critical+High 計數為 0；`git log --oneline -1` 顯示符合台灣繁中慣例之 emoji commit 訊息。

## 2. 建立 zh-TW data loader、切換 markdown import、修補 formatDate locale

實作設計決策「Per-locale data loader 採「新增 zh-TW loader + markdown 頁面切換 import」」與「`formatDate` locale 取得方式採「`useI18n()` 直接讀 `locale.value`」」，並落地需求「Challenge data loader is locale-aware via per-locale data files sharing one ChallengeData type」與「Component formatDate respects active i18n locale」。

- [x] 2.1 建立 `docs/shared/challenges.zh-TW.data.ts`，內部 `import type { ChallengeData } from './challenges.data'`、`createContentLoader('zh-TW/challenge/*/index.md', ...)`、`transform` 與 `challenges.data.ts` shape 對等、fallback 保持繁中（`'網站攻防挑戰'`、`'綜合'`）。本 task 對應 design.md「Implementation Contract」之「interface / data shape」段落（新增模組之 default export、`ChallengeData[]` 結果型別、與英文 loader 共用之介面契約）。交付行為：需求「Challenge data loader is locale-aware via per-locale data files sharing one ChallengeData type」之 scenario「Traditional Chinese pages load zh-TW challenge data」與「ChallengeData type is defined once and reused」之資料源就緒。Verification：`pnpm exec tsc --noEmit` 對該檔無型別錯誤；`pnpm docs:build` 後生成的 `_assets/challenges.zh-TW.data.*` chunk 存在且包含至少一筆 challenge 資料（door-is-open）。
- [x] 2.2 清理 `docs/shared/challenges.data.ts` fallback：`title` `'網站攻防挑戰'` → `'Web exploitation challenge'`、`category` `'綜合'` → `'General'`；`ChallengeData` 介面定義與 `createContentLoader` glob、closed difficulty union 全不動。交付行為：英文 loader 在 frontmatter 缺漏時退化值為英文，符合需求「Challenge data loader is locale-aware via per-locale data files sharing one ChallengeData type」之 scenario「Closed difficulty union remains enforced across both loaders」對英文 loader 端的維持。Verification：`rg '網站攻防挑戰\|綜合' docs/shared/challenges.data.ts` exit 1；`pnpm exec tsc --noEmit` 無型別錯誤。
- [x] 2.3 切換 `docs/zh-TW/index.md` 之 `<script setup>` import：`import { data } from '../shared/challenges.data.ts'` → `'../shared/challenges.zh-TW.data.ts'`；`<HomeContent :challenges="data" />` 用法不動。交付行為：繁中首頁顯示繁中 challenge 卡片，達成需求「Challenge data loader is locale-aware via per-locale data files sharing one ChallengeData type」scenario「Traditional Chinese pages load zh-TW challenge data」對 `/zh-TW/` 路由的部分。Verification：`pnpm docs:dev` 開 `/zh-TW/` 看到「門已敞開」卡片標題；`rg "challenges\\.zh-TW\\.data" docs/zh-TW/index.md` 命中。
- [x] 2.4 切換 `docs/zh-TW/challenges.md` 之 `<script setup>` import 同樣由 `'../shared/challenges.data.ts'` 改為 `'../shared/challenges.zh-TW.data.ts'`。交付行為：繁中 ChallengeList 顯示繁中卡片，完成 scenario「Traditional Chinese pages load zh-TW challenge data」對 `/zh-TW/challenges` 路由的部分。Verification：`pnpm docs:dev` 開 `/zh-TW/challenges` 看到「門已敞開」並能正常 sort/filter；`rg "challenges\\.zh-TW\\.data" docs/zh-TW/challenges.md` 命中。
- [x] 2.5 修 `.vitepress/theme/components/HomeContent.vue` 之 `formatDate`：新增 `import { useI18n } from 'vue-i18n'`、於 `<script setup>` 取 `const { locale } = useI18n()`、`new Date(d).toLocaleDateString(locale.value === 'zh-TW' ? 'zh-TW' : 'en-US', { year: 'numeric', month: 'short', day: 'numeric' })`；元件 props / emits / template 不動。交付行為：需求「Component formatDate respects active i18n locale」之 scenario「English locale produces English-format dates」與「Traditional Chinese locale produces Chinese-format dates」於 HomeContent 之 latest challenges 卡片日期成立。Verification：`pnpm docs:dev` 開 `/` 看到日期形如 `Apr 2, 2026`、開 `/zh-TW/` 看到日期含 `年` / `月` CJK 字元；`pnpm test --run` 失敗數不超過既有 5 個。
- [x] 2.6 修 `.vitepress/theme/components/ChallengeList.vue` 之 `formatDate` 同樣套用 locale-aware pattern；元件 template、props、defineProps 不動。交付行為：需求「Component formatDate respects active i18n locale」之 scenario「Locale switch re-renders dates without reload」於 ChallengeList 卡片日期成立（搭配 LocaleSwitcher 雙向切換即時更新）。Verification：開 `/challenges` 與 `/zh-TW/challenges`，日期格式各自英文 / 繁中；於同一頁面點 LocaleSwitcher 切換語系，日期格式即時更新無 page reload。
- [x] 2.7 Stage 2 收尾：執行 `/spectra-audit`，Critical+High = 0 後 `/tw-emoji-commit`。Verification：`spectra analyze content-i18n-migration --json` Critical+High = 0；`pnpm test --run` 失敗數 = 5（AUDIT.md §A.3 既知 CodeEditorPanel 5 個 known-fail，不退化）。

## 3. 撰寫 docs/guide/index.md 真實英文 Getting Started 內容

實作 proposal 之 What Changes「撰寫 `docs/guide/index.md` 真實英文 Getting Started 內容」，並完成 Change 2 留下之 placeholder 清理。

- [x] 3.1 以 `docs/zh-TW/guide/index.md`（76 行）為結構藍本，重寫 `docs/guide/index.md` 為自然英文 Getting Started 文件。標題層級、code block、列表項與 zh-TW 來源 1:1 對應；技術術語英文原文（VitePress、WebAssembly、Pyodide）保留；刪除原 placeholder 之 `<!-- TODO: full content migration in change content-i18n-migration -->` 與「The English version of this page is a placeholder」說明。交付行為：英文 `/guide/` 路由顯示完整英文 Getting Started 文件，與 zh-TW 對應頁面結構齊備。Verification：`rg '[一-鿿]' docs/guide/index.md` exit 1；`rg 'TODO\|placeholder' docs/guide/index.md` exit 1；`pnpm docs:build` 後 `dist/guide/index.html` 不含 placeholder 字串、行數 ≥ 60。
- [x] 3.2 Stage 3 收尾：執行 `/spectra-audit` + `/tw-emoji-commit`。Verification：`spectra analyze content-i18n-migration --json` Critical+High = 0；commit 訊息符合台灣繁中慣例。

## 4. 撰寫其餘三個 guide 頁的真實英文內容（[P]）

實作 proposal 之 What Changes「撰寫 `docs/guide/python.md`、`docs/guide/terminal.md`、`docs/guide/network.md` 真實英文內容」。三檔互不依賴，可並行。

- [ ] 4.1 [P] 重寫 `docs/guide/python.md`（對應 `docs/zh-TW/guide/python.md` 197 行）：Code Editor、Pyodide modules、requests API、attack examples。code block 與註解逐字搬遷；技術術語英文原文保留。交付行為：英文 `/guide/python` 路由顯示完整英文 Python guide。Verification：`rg '[一-鿿]' docs/guide/python.md` exit 1；`rg 'TODO\|placeholder' docs/guide/python.md` exit 1；`pnpm docs:build` 後 `dist/guide/python.html` 行數 ≥ 150。
- [ ] 4.2 [P] 重寫 `docs/guide/terminal.md`（對應 `docs/zh-TW/guide/terminal.md` 223 行）：wxlsh 指令文件。所有 wxlsh subcommand 名稱與旗標保持原樣（如 `wxlsh curl`、`-X POST`、`--data` 等），僅描述文字翻譯。交付行為：英文 `/guide/terminal` 路由顯示完整英文 wxlsh 指令參考。Verification：`rg '[一-鿿]' docs/guide/terminal.md` exit 1；`rg 'wxlsh' docs/guide/terminal.md` 命中數 ≥ `rg 'wxlsh' docs/zh-TW/guide/terminal.md` 命中數（指令名稱不丟失）；`pnpm docs:build` 後 `dist/guide/terminal.html` 行數 ≥ 170。
- [ ] 4.3 [P] 重寫 `docs/guide/network.md`（對應 `docs/zh-TW/guide/network.md` 143 行）：Network panel + Repeater workflow + examples。交付行為：英文 `/guide/network` 路由顯示完整英文 Network & Repeater guide。Verification：`rg '[一-鿿]' docs/guide/network.md` exit 1；`rg 'TODO\|placeholder' docs/guide/network.md` exit 1；`pnpm docs:build` 後 `dist/guide/network.html` 行數 ≥ 110。
- [ ] 4.4 Stage 4 收尾：執行 `/spectra-audit` + `/tw-emoji-commit`。Verification：`spectra analyze content-i18n-migration --json` Critical+High = 0；commit 訊息符合台灣繁中慣例。

## 5. 完整 pipeline 驗收與終局雙層稽核（artifact + code）

實作設計決策「五階段切分搭配雙層稽核 gate」之 stage 5 收尾。

- [ ] 5.1 全域 CJK gate：執行 `rg '[一-鿿]' docs/index.md docs/challenges.md docs/guide/ docs/challenge/door-is-open/index.md docs/shared/challenges.data.ts`，要求 exit 1（0 hits 跨英文側 markdown 與英文 loader）。交付行為：英文 root 樹完全無 CJK 殘留，符合 Change 2 之既有 CJK gate 風格但擴及內容遷移後的完整檔案集合。Verification：上述 `rg` 指令 exit code 為 1。
- [ ] 5.2 完整 build + test pipeline 驗收：依序執行 `pnpm install`、`pnpm wasm:build`、`pnpm challenge:keygen`、`pnpm docs:build`、`pnpm test --run`、`pnpm challenge:validate`、`cargo test --workspace`。交付行為：i18n 內容遷移後完整工具鏈仍可運作、且不退化 Change 2 既有 i18n 測試。Verification：除 AUDIT.md §A.3 既知 5 個 `CodeEditorPanel` 失敗外，所有指令 exit 0；`tests/unit/i18n/*.test.ts` 全綠；`dist/` 同時存在 `index.html` 與 `zh-TW/index.html`，兩者 `<html lang>` 各自為 `en-US` / `zh-TW`；`dist/challenge/door-is-open/index.html` 與 `dist/zh-TW/challenge/door-is-open/index.html` 兩檔皆存在。
- [ ] 5.3 手動雙語切換瀏覽器煙霧測試 14 個路由：`/`、`/zh-TW/`、`/challenges`、`/zh-TW/challenges`、`/guide/`、`/zh-TW/guide/`、`/challenge/door-is-open/`、`/zh-TW/challenge/door-is-open/`、`/guide/python`、`/zh-TW/guide/python`、`/guide/terminal`、`/zh-TW/guide/terminal`、`/guide/network`、`/zh-TW/guide/network`。每路由：點 LocaleSwitcher 雙向切換、確認 URL 對映正確、`localStorage.wxl-locale` 寫入、首頁日期格式隨 locale 即時切換、challenge 卡片標題與描述語言切換。本 task 驗證 design.md「Implementation Contract」之「observable behavior」段落所列 6 項使用者可觀察行為（English/zh-TW root hero、ChallengeList 雙語顯示、guide 頁完整英文、door-is-open 雙語題目敘述、LocaleSwitcher 雙向切換、日期格式 reactive 更新）逐項對位。交付行為：14 路由全綠之 i18n 端到端體驗（runtime + content + 元件 formatDate）一致達成。Verification：在 commit 訊息或 archive 註記文件中以條列方式記錄 14 路由觀察結果，每項標 PASS / FAIL；任一 FAIL 必須回頭修正後重跑。
- [ ] 5.4 Artifact 層稽核：執行 `spectra analyze content-i18n-migration --json`，要求 Critical+High finding 計數為 0；執行 `spectra validate content-i18n-migration`，要求 exit 0。本 task 對應 design.md「Implementation Contract」之「acceptance criteria」段落第 6 項（雙層稽核 gate）對 artifact 層的要求。交付行為：本 change 之 4 個 artifact（proposal、design、specs、tasks）通過 Spectra 內建一致性檢查、達成 acceptance criteria 之 artifact 層門檻。Verification：兩指令依序 exit 0 且 analyze 之 JSON 內 `findings[].severity in ['Critical','High']` 過濾結果為空陣列。
- [ ] 5.5 Code 層稽核（強制收尾 gate）：對本 change 累計 diff（stage 1–4 全部 commit 之累積變更）執行 `/spectra-audit` skill，掃描範圍涵蓋 dangerous defaults（`challenges.zh-TW.data.ts` fallback 字串、`toLocaleDateString` 未知 locale 退化）、type confusion（`ChallengeData` 跨檔 re-import、closed difficulty union）、silent failures（兩棵 challenge tree slug 對齊性、zh-TW frontmatter `date` 缺漏、`createContentLoader` glob 失配退化）、stringly-typed risks（markdown import path 字串、slug / URL 對映）、workflow bypass（stage 依賴順序）。本 task 對應 design.md「Implementation Contract」之「failure modes」段落（4 項退化行為契約：zh-TW loader glob 不命中、frontmatter date 缺漏、SSR useI18n、未知 locale 退化）與「scope boundaries」段落（明確列出 in scope 與 out of scope 之檔案集合）的 code 層強制檢查。交付行為：i18n 內容遷移之程式碼層級安全與穩定性（含 failure modes 與 scope boundaries）已被獨立 audit lens 檢視；Critical+High = 0 為本 change 收尾與後續 archive 之必要條件。Verification：`/spectra-audit` 報告中 Critical+High finding 數為 0；Medium 與 Low finding 之處置（accept / defer / reject）已在 design.md 之 Risks/Trade-offs 章節或本 stage 之 commit message 中明確記錄。
- [ ] 5.6 終局 commit：執行 `/tw-emoji-commit` 收尾 stage 5 之所有修正與 audit 紀錄。交付行為：本 change 之 stage 1–5 commit chain 完整、最末 commit 訊息含「`/spectra-audit` 通過（Critical+High = 0）」字樣以供 archive 階段追溯。Verification：`git log --oneline content-i18n-migration..HEAD` 顯示完整 stage 1–5 commit chain；最末 commit message 含上述驗證字樣；`spectra validate content-i18n-migration` exit 0。
