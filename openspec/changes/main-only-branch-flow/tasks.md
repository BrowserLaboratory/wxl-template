## 1. main-only 程式碼編輯（兩檔互不相依，可平行）

- [x] 1.1 [P] 把 `CONTRIBUTE.md` 全面改為 main-only，交付 "CONTRIBUTE describes git flow branch model"（本次改為 main-only 內文）與 "CONTRIBUTE describes PR submission process" 兩條 Requirement：branch model 表移除 `staging` row、`feature/*`/`bugfix/*`/`hotfix/*` 基底欄改 `main`；development workflow 改從 `main` 切工作分支（`git checkout main` / `git pull upstream main`）；PR submission 的 target 表與 PR checklist 一律以 `main` 為 PR 目標；hotfix 改為從 `main` 切、PR 合回 `main`（移除雙合 staging 的敘述與示意圖中的 staging 線）；並移除 Maintainer Setup「Protect main」段中「`staging` … tracked as a separate change」字句。驗證：`rg -n -i 'staging' CONTRIBUTE.md` 無 staging-分支 語意命中（git 暫存區語意不算）、content review 對照兩條 Requirement 的 scenario、`pnpm docs:build` 成功（`CONTRIBUTE.md` 屬 outward docs，須通過 prose-audit blocking set）。
- [x] 1.2 [P] 把 `.github/workflows/quality-gates.yml` 的 `on.pull_request.branches` 由 `[main, staging]` 改為 `[main]`（`push.branches` 本已只 `main`），交付 "PR-time quality gates SHALL trigger on PRs to `main` and pushes to `main`" Requirement 的觸發契約，取代已移除的 "PR-time quality gates SHALL trigger on PRs to `main`/`staging` and pushes to `main`" Requirement。驗證：`rg -n -i 'staging' .github/workflows/quality-gates.yml` 無命中；workflow YAML 仍可被 GitHub Actions 正常解析（語法正確、test/build/prose-audit/site-smoke job 結構不變）。
