{
  "$comment": "Vendored from ~/.claude/skills/humane-prose-audit/schemas/check-output.schema.json @ 2026-05-28; do not edit in place. Upstream-sync via dedicated change.",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://humane-prose-audit/schemas/check-output.schema.json",
  "title": "check-output",
  "description": "Output contract for any humane-prose-audit deterministic check (humane-prose-audit/checks/*.py). Emitted on stdout as a single JSON object.",
  "type": "object",
  "required": ["check", "target", "findings"],
  "additionalProperties": false,
  "properties": {
    "check": {
      "type": "string",
      "minLength": 1,
      "description": "Stable rule identifier (e.g. ai_tells, burstiness, citation_format)."
    },
    "target": {
      "type": "string",
      "minLength": 1,
      "description": "Absolute path to the audited file."
    },
    "findings": {
      "type": "array",
      "items": { "$ref": "#/definitions/finding" }
    },
    "metadata": {
      "type": "object",
      "description": "Optional check-specific metadata (e.g. computed score, thresholds, locale)."
    },
    "nondeterministic": {
      "type": "boolean",
      "description": "True iff the check could not honor a requested seed."
    }
  },
  "definitions": {
    "finding": {
      "type": "object",
      "required": ["severity", "section", "issue", "evidence"],
      "additionalProperties": false,
      "properties": {
        "severity": {
          "type": "string",
          "enum": ["critical", "high", "medium", "low"]
        },
        "section": { "type": "string" },
        "issue": { "type": "string", "minLength": 1 },
        "evidence": { "type": "string" },
        "wiki_refs": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^wiki/concepts/[^/]+/[^/]+\\.md$"
          }
        },
        "metadata": { "type": "object" }
      }
    }
  }
}
